# Local Development Environment Variables
# Copy this file to .env.local and fill in your values

# Database (Local PostgreSQL via Docker)
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/orizonqa

# Redis (Local Redis via Docker)
REDIS_URL=redis://localhost:6379

# Next-Auth
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3033
NEXTAUTH_SECRET=your-secret-key-here

# Email Service (Resend)
# Get your API key from https://resend.com
RESEND_API_KEY=re_xxx

# Encryption for API Keys
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your-32-char-encryption-key

# Production on Vercel:
# - POSTGRES_URL is auto-configured by Vercel Postgres
# - KV_REST_API_URL and KV_REST_API_TOKEN are auto-configured by Vercel KV
# - Set NEXTAUTH_URL to your production domain (https://your-app.vercel.app)
# - Set NEXTAUTH_SECRET, RESEND_API_KEY, and ENCRYPTION_KEY in Vercel project settings
